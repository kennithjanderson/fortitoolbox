services:
  fortitoolbox:
    image: ghcr.io/kennithjanderson/fortitoolbox:latest
    container_name: fortitoolbox
    ports:
      - "8088:8080"  # Change 8088 if your host already uses it
    restart: unless-stopped
